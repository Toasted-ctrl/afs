[project]
name = "afs"
version = "0.1.0"
description = "A collection of Apache AirFlow DAGs and shared resources."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "apache-airflow>=3.3.1",
]
